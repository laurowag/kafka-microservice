swarm:
  datasources:
    data-sources:
      ConexaoDS:
        driver-name: sqlserver
        connection-url: jdbc:sqlserver://<IP SQL Server>:1433;DatabaseName=<nome database>
        user-name: <username>
        password: <password>